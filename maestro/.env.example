# Copy to .env (gitignored) and fill in real values before running flows.
# These are consumed by flows via the $ENV_VAR placeholder syntax in YAML.

# Staging account credentials for login flow.
TEST_EMAIL=
TEST_PASSWORD=

# Optional: a free-tier account if a flow needs to assert paid-only restrictions.
TEST_FREE_EMAIL=
TEST_FREE_PASSWORD=
